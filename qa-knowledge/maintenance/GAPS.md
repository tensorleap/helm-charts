# QA KB — gap log

Append-only log of maintenance runs and open gaps. Newest entry on top. Each run
(scheduled or reactive) adds a dated section: what was reviewed, what changed, and
any gaps that could not be auto-resolved (for a human to act on).

Format:
```
## YYYY-MM-DD — <scheduled | reactive | initial>
Reviewed: <repos + sha ranges, or "n/a">
Changed: <doc — one-line why> (repeat per doc; "none" if no drift)
Open gaps: <doc/topic — what's uncertain and why> ("none" if clean)
```

---

## 2026-09-09 — manual refresh (all repos → master)
Reviewed: helm-charts `5056f5bd..5c8a0db2` (CI workflow, `WORKFLOW_DIAGRAM.md`, a release-image-pinning script, and the prior KB-update PR's own doc edits — no manifest-glob match), engine `5f1b17ba..0bb5016a`, node-server `5a89a14f..b1836f98`, web-ui `8a9b274c..fe1e1d88`, leap-cli `3e6d45c4..edd3bab8` (incl. a full generated-API-client regen, OpenAPI spec `11.0.133`→`11.0.151`); code-loader unchanged (baseline SHA == current `origin/master` HEAD). All baselines were ancestors — clean diffs. 8 per-doc verifiers across 02, 03, 04, 05, 06, 07, 08, 09 (01/10 skipped — no manifest-glob match; a targeted research pass covered the cross-cutting feature first).
Changed: 09-job-catalog.md — new 8th `SLIM_LS` request type **Unlabeled Analysis** (`WorkerSlimLSOps.unlabeled_analysis_task` → `UnlabeledAnalysis.run_unlabeled_analysis`; mongo `unlabeledanalysis`; bucket `vis/<vis_artifact_id>/unlabeled_analysis/<jobId>/stats.json`, keyed by job id like Domain Gap, **not** the `digest_<d>` pattern other DS-Curation subtypes use): new catalog table row + a full detail section (trigger/endpoint, spawn shape, outputs, success criteria, top-panel application, a dispatch-shim gotcha). Also fixed a tab-label reshuffle this feature caused: the DS Curation dialog's pre-existing tab (value `UNLABELED`, backs Labeling Recommendation, `UnlabeledTabContent.tsx`) is now labeled **"LABEL NEXT"**, and the label **"UNLABELED"** moved to the new tab (value `UNLABELED_ANALYSIS`, `UnlabeledAnalysisTabContent.tsx`) — corrected 4 stale "UNLABELED tab" references that would otherwise now point at the wrong feature, and updated "five tabs" → "six tabs".
Changed: 04-job-types-and-lifecycle.md — added `Unlabeled Analysis` to the three places that enumerate `SLIM_LS`/`WorkerSlimLSOps` subtypes (node-server `JobSubType` list, job-dispatch table, runtime-resources table); confirmed it's a plain single-`SLIM`-pod dispatch like its siblings, no new pod/fan-out shape, `k8s.ts` job-naming rule unaffected.
Changed: 02-components.md — added `unlabeledanalysis` to the enumerated mongo-collections list (new `unlabeledAnalysisDb`, `node-server/src/dataset-curation/db.ts`); updated `leap push -u/--update` value list from `{metadata|metric|metric_config|viz}` to `{metadata|metric|metric_config|viz|samples}` (new `samples` value, `leap-cli/pkg/model/evaluate.go` + `cmd/root_cmd/push.go`).
Changed: 08-test-playbooks.md — same `-u/--update … samples` addition in the "Overwrite / sub-version" playbook, plus its behavior: evaluates only newly-added sample ids via continue-evaluate (existing ids must stay stable, a shrink is rejected) and always runs immediately, skipping the run-eval prompt (unlike other update values without `--eval`).
Changed: none (03-data-flows.md, 05-testing-utils.md, 06-ui-inspection.md, 07-failure-modes.md) — reviewed against the same diff set (Unlabeled Analysis engine/trainer logic, substantial `web-ui/src/dashboard/**` changes, node-server `jobs`/`utils` tweaks, the leap-cli API-client regen). The new feature rides entirely on existing SLIM_LS dispatch/failure/status machinery (no new failure mode, no changed job-status lifecycle); none of these docs' cited test-ids, selectors, queue names, or flow steps were invalidated. Notable non-doc-breaking changes seen but not cited by any doc: web-ui's `LatentSpaceSyncContext` renamed to `PopulationExplorationSyncContext` (+ a new `topBar` prop on `ScatterMap`/`ScatterAnalyzerView`); node-server's `evaluate/logic.ts` internal `routeBySampleChanges` refactor.
Open gaps: (1) **new** — `WorkerSlimLSOps._unlabeled_analysis_request` (engine `workerslimlsops.py`) still contains a fallback path that re-tags a `synthetic_calibration`-shaped request as `unlabeled_analysis`, documented in its own docstring as a transitional shim from before node-server could send the real type; node-server's `generateUnlabeledAnalysis` already sends `slimRequestType: 'unlabeled_analysis'` directly, so the shim is likely dead in practice — worth a live-cluster spot-check if an Unlabeled Analysis job ever appears to run with an unexpected request shape. (2) **new, out of KB scope** — engine's own `helm-chart/templates/{engine-job-template-cm,engine-orchestrator}.yaml` gained an optional FSx-storage PVC mount gated by `.Values.fsx.enabled`; this is engine's SaaS deployment chart (not a `helm-charts` repo path our manifest tracks), and looks unrelated to the local k3d install this KB documents — flagging only in case FSx-backed storage ever becomes relevant to self-hosted installs. (3) carried from 2026-09-08: pruning top-panel stats file (`dataset_balancing_stats.json`) still not produced by engine master this cycle either — re-verify once `origin/pruning` (or successor) merges. (4) carried from 2026-08-31: `synthetic_top_panel.json` frontend consumption unconfirmed; `editor-file-name-row`/`editor-rename-file-input` dead test-ids in 06-ui-inspection.md; node-server job-history page-size 100 unreferenced by any doc.

## 2026-09-08 — manual refresh (all repos → master)
Reviewed: helm-charts `adaf858b..5056f5bd` (image/chart version bumps + prior KB-update PR #441's own doc edits), engine `2495ead9..5f1b17ba`, node-server `694215a2..5a89a14f`, web-ui `41d03008..8a9b274c` (all baselines were ancestors — clean diffs; 8 per-doc verifiers across 01, 02, 03, 04, 05, 06, 07, 09; leap-cli and code-loader unchanged this cycle, 08/10 skipped — no manifest-glob match).
Changed: 01-architecture.md — routine snapshot refresh: image tags (web-ui→`master-8a9b274c`, node-server→`master-5a89a14f`, engine/engine-generic→`master-41be9147`), chart `1.6.74`→`1.6.76` (infra unchanged `1.1.8`).
Changed: 04-job-types-and-lifecycle.md — added a "Multi-GPU Evaluate memory" bullet: node-server's `applyMultiGpuMemoryAdjustment` (`getMainPodEvaluateSettings` in `node-server/src/utils/engine.ts`) now adds `MULTI_GPU_MEMORY_GB_PER_EXTRA_GPU` (2 GiB) per GPU beyond the first to `memory_required_gb`/`memory_limit_gb` when auto-settings batch size splits evenly across GPU count, mirroring the engine's `LeapDistStrategy` `MirroredStrategy` gate — closes an under-provisioning gap the existing OOM-knobs list didn't cover.
Changed: none (02-components.md) — engine's concept_heatmap/domain_gap/calibration_optuna/workersynthetic/workerpush/feature_flags/leapdiststrategy changes, node-server's `contact.ts`/`engine.ts` tweaks, and all web-ui diffs (useResize, DashboardDrawer/Layout/Pane, ScatterAnalyzerView, InsightCardText, sample-collection table/dialog, RegistrationDialog, Button atom) are internal deepenings below this doc's component-level granularity; none introduce a new top-level component.
Changed: none (03-data-flows.md) — same changed-file set reviewed against cited flows (RabbitMQ job creation, `LeapTrainer` post-processing, Collections drawer indexing badge, PE domain-gap request params); all remain accurate, no observable-level drift.
Changed: none (05-testing-utils.md, 06-ui-inspection.md, 07-failure-modes.md, 09-job-catalog.md) — reviewed against the same diff set (import-model/workersynthetic sizing-constant tuning, contact.ts await fix, dashboard drawer resize feature, sample-collection loading-spinner conditions); no cited test-id, selector, queue name, failure symptom, or job name/subtype/step was invalidated.
Open gaps: (1) carried from 2026-09-07: pruning top-panel stats file still not produced by engine (re-verify once `origin/pruning` merges). (2) carried from 2026-08-31: `synthetic_top_panel.json` frontend consumption unconfirmed; `editor-file-name-row`/`editor-rename-file-input` dead test-ids in 06-ui-inspection.md; node-server job-history page-size 100 unreferenced by any doc. (3) new, optional: 07-failure-modes.md doesn't yet mention multi-GPU Evaluate memory headroom (`MULTI_GPU_MEMORY_GB_PER_EXTRA_GPU`) as an OOM-prevention mechanism — not added since it wasn't correcting existing wrong content, but a future pass could cross-reference the new 04-job-types-and-lifecycle.md bullet.

## 2026-09-07 — manual refresh (web-ui/node-server → master)
Reviewed: web-ui `7d987047..41d03008` (3 commits), node-server `9f1ef94b..694215a2` (2 commits); engine, leap-cli, code-loader unchanged (baseline SHA == current `origin/master` HEAD); helm-charts `7c6fd166..adaf858b` touched only `qa-knowledge/**` (the prior KB-update PR) — no manifest-glob match.
Changed: 09-job-catalog.md — "Pruning top panel" (node-server #1834 / web-ui #3464) gives Dataset Balancing a stats file + a 4th dashboard top-panel kind, mirroring Labeling Recommendation: (a) bucket-output row for Dataset Balancing now also lists `dataset_balancing_stats.json` (node-server `DATASET_BALANCING_STATS_FILENAME`, checked/exposed as `statsFileUrl` same as Synthetic already documented); (b) new bullet describing the PRUNING tab's "Apply as dashboard top panel" action (`applyPruningTopPanel`/`PruningTopPanel.tsx`, kind `'pruning'` in the TOP_PANEL slot) with a flagged gotcha that it's **dormant today** — engine `master` has no `get_dataset_balancing_stats_path`/writer for that file yet (in progress on remote branch `origin/pruning`, not merged), so `statsFileUrl` is never set and the UI silently falls back to the pre-existing "Apply filter to Population Exploration" action.
Changed: none (node-server migration 147) — data-only backfill (`has_element_instances` on legacy `codesnapshots`, engine contract 0.0.482) for a schema-validation bug (`DomainGapRequest` rejected setups missing the field); runs automatically via the existing migrations init-container on upgrade (already-documented mechanism), self-heals, and no doc asserts on this field — no drift.
Changed: none (web-ui #3469/#3467) — bar-chart hover now outlines the bar instead of drawing a recharts cursor line, and the Notes button tooltip now shows note content; both pure visual polish with no doc claims about chart-cursor or tooltip behavior to update.
Changed: none (node-server `sampleIdKey`/web-ui refactor) — unified three previously-inconsistent internal cache-key separators (`-`, `:`, `_`) onto the engine's own `${state}_${index}` format via a new shared `sampleIdKey` helper in both repos; no doc cites the separator format, so nothing to patch.
Open gaps: (1) **Pruning top panel stats file not yet produced by engine** (see above) — re-verify once `origin/pruning` (or successor) merges to engine master; expect a doc update removing the "dormant" caveat and confirming the coverage-curve/metadata-distribution rendering actually appears. (2) Not re-derived from source this run, carried from 2026-08-31: synthetic_top_panel.json frontend consumption, `editor-file-name-row`/`editor-rename-file-input` dead test-ids, node-server job-history page-size 100.

## 2026-09-06 — manual refresh (web-ui/node-server → master)
Reviewed: web-ui `2c66889b..7d987047` (2 commits), node-server `afa88487..9f1ef94b` (1 commit); engine, leap-cli, code-loader unchanged (baseline SHA == current `origin/master` HEAD); helm-charts `3a4f3072..7c6fd166` touched only `qa-knowledge/maintenance/**` (the prior KB-update PR) — no manifest-glob match.
Changed: 09-job-catalog.md — added a bullet under Labeling Recommendation: applying an UNLABELED-tab row as the dashboard top panel (`applyUnlabeledTopPanel`) no longer leaves the Population Exploration dashlet filtered to just the suggested samples like every other dashlet — it now swaps in the recommendation's balanced/`filterFileUrl` cluster blob (`useLabelingPopulationClusterSwap` in `web-ui/src/dashboard/top-panel/useTopPanelState.ts`) so labeled/suggested/not-chosen populations all stay visible together (web-ui #3465).
Changed: none (node-server) — the only node-server commit in range (#1837) is a pure directory rename `src/analysis-export` → `src/z_analysis-export` (tsoa route-registration ordering fix) plus an api-client regen; the `@Route('analysis-export')` URL path 02-components.md cites is unaffected, and web-ui's matching "fix api client" commit (#3466) only touches Storybook mocks/stories (a new `has_element_instances` mock field) with no behavior change.
Open gaps: none new.

## 2026-09-06 — manual refresh (all repos → master; no drift)
Reviewed: helm-charts `ea15dbbf..3a4f3072` (8 changed files, all under `qa-knowledge/maintenance/**` and `.claude/skills/update-qa-knowledge/**` from the prior KB-update PRs — none match a manifest glob); engine, node-server, web-ui, leap-cli, code-loader all unchanged (baseline SHA == current `origin/master` HEAD).
Changed: none — no doc depends on the helm-charts paths that changed (docs/skill-maintenance files only, no `charts/`, `pkg/`, `cmd/`, or `images.txt` touched).
Open gaps: none new. helm-charts baseline bumped to `3a4f3072`; other 5 repos unchanged so their baselines were left as-is.

## 2026-08-31 — manual refresh (all repos → master)
Reviewed: helm-charts `f8fabefc..ea15dbbf`, engine `57b666e0..2495ead9`, node-server `49f874da..afa88487`, web-ui `ffbbadac..2c66889b`, leap-cli `99a21833..3e6d45c4`, code-loader `43d6434e..3b5127b7` (all baselines were ancestors — clean diffs; 9 per-doc verifiers).
Changed: 01-architecture.md — image tags (web-ui→`master-02476675`, node-server→`master-74d20119`, engine/engine-generic→`master-ee2324f1`), chart `1.6.71`→`1.6.74`. 02-components.md — node-server gained a new top-level `/analysis-export` REST module (`listTargets`/`exportAnalysis`/`getSampleAssets`, versioned via `ANALYSIS_EXPORT_CONTRACT_VERSION`) consumed by the external `tensorleap-analysis` skill, not the web-ui. 07-failure-modes.md — added a row: web-ui `AuthProvider.tsx`/`api-client.tsx` no longer force sign-out on a transient token-refresh rejection or a `checkLoginIframe` timeout (both now warn-and-retry); only a genuine `onAuthLogout` ends the session — previously undocumented, could confuse triage of "unexpected sign-out" reports. 08-test-playbooks.md & 09-job-catalog.md — `leap-cli push.go` added `wantsNewVersion()` (EN-2462): `-n/--name` without `-o/--overwrite` now always creates a new version and skips the interactive overwrite prompt. 09-job-catalog.md — both manual (`WorkerSlimLSOps.synthetic_calibration`) and auto (`WorkerSyntheticJob`) Synthetic Data Generation flows now additionally write a `synthetic_top_panel.json` blob, exposed by node-server as `SyntheticData.statsFileUrl`. 03-data-flows.md, 04-job-types-and-lifecycle.md, 05-testing-utils.md, 06-ui-inspection.md — no drift found; all cited symbols/queue names/test-ids/enums confirmed unchanged on current master.
Open gaps: (1) **`synthetic_top_panel.json` / `statsFileUrl` frontend consumption unconfirmed** — could not verify from source whether the web-ui SYNTHETIC tab actually renders new UI from this field yet, or if it's plumbed but unused; needs a live-cluster check. (2) `qa-knowledge/06-ui-inspection.md` still lists `editor-file-name-row`/`editor-rename-file-input` as "actually-applied" test-ids, but they're dead code in current web-ui master too — pre-existing doc inaccuracy, unrelated to this diff range, worth a follow-up fix. (3) node-server's `src/jobs/db.ts` job-history query limit changed `20`→`100` — checked against every doc's citations, not referenced by name anywhere, so no doc touched, but flagging in case a future playbook starts asserting on job-list page sizes.

## 2026-08-23 — manual refresh (all repos → master; first full verify of leap-cli & code-loader)
Reviewed: helm-charts `05643d36..f8fabefc`, engine `026729cf..57b666e0`, node-server `9276bb7c..49f874da`, web-ui `f16249f1..ffbbadac` (all baselines ancestors — clean diffs); leap-cli `99a21833` and code-loader `43d6434e` full-verified from null baselines (10 per-doc verifiers, sources pinned at origin/master worktrees).
Changed: 01-architecture.md — image tags (engine/engine-generic→`28b2652b` + new py311 variant, node-server→`63808c73`, web-ui→`c8508269`, pippin→`f979190d`), chart `1.6.33`→`1.6.71`. 02-components.md — job creation now published to RabbitMQ `engine-job-creation` (orchestrator creates the Job); node-server liveness moved to always-200 `/monitor/alive` (BF-1173), 25 controllers, `setEngineGenericWorkers` removed (engine settings → `/settings`); new mongo collections `datasetsplitting`/`domaingap`; streaming-handler cap 20 / bulk 1000 / computed memory; redis hook + scriptcommand queue rows; web-ui `/panel/<tab>` path segment + live `data-testid`s; installer `--gpus --gpu-devices`, `uninstall --custom`; leap-cli `auth license`, `projects init`, `run info`; code-loader `LeapDataType.Audio`. 03-data-flows.md — Flow A rewritten for orchestrator/RabbitMQ job creation (UNSTARTED→PENDING via `active_jobs_report`), sharded streaming queues (`_s<N>_`, default 8), bulk ≤1000, Flow C page size 10_000. 04-job-types-and-lifecycle.md — new `QUEUED` status (memory admission), orchestrator `JobCreationConsumer` creates Jobs, `Resplitting`→`Splitting`, `Domain Gap` ANALYZE subtype, manual-vs-auto Synthetic. 05-testing-utils.md — `leap server tools kubectl <args>` (kubectl is a named subcommand), MinIO console port-forward must target `deploy/` (svc lacks 9001). 06-ui-inspection.md — dead `hub-gallery` removed, drawer nav `/panel/<tab>`, dashlet loading is a spinner not `Loading...`, aria-label count ~35. 07-failure-modes.md — `k8s engine-job-config was not found` log text, sharded `LLEN`, streaming replicas cap 20, ES 503 disk message. 08-test-playbooks.md — `leap run` inspects only; stop/terminate is UI (or the CLI's concurrent-limit picker). 09-job-catalog.md — Domain Gap family added (trigger/bucket/mongo), Splitting shipped end-to-end, Synthetic split into manual SLIM_LS vs auto SYNTHETIC rows, DS Curation now 5 tabs, PE gates on `prepare_displays` not `insights_analysis`, curation bucket filenames corrected. 10-verification-toolbox.md — no drift. README.md — intro no longer says node-server creates the Jobs (mental-model point 2 already covered it).
Open gaps: (1) **ttlSecondsAfterFinished divergence** — engine repo's own `helm-chart/templates/engine-job-template-cm.yaml` sets 300, but the shipping chart here still sets 36000; docs keep 36000 (correct for installs today) — update 04/07 if helm-charts syncs the template. (2) **Optional body-Redis pod** `redis-<jobId>-body` (label `jobType: redis-body-shards`, engine `deployment_manager.py:389-442`) is skipped in default config (`EVAL_BATCH_REUSE_STORAGE` routes bodies to the shared volume) — not yet documented in 02/04; document if it becomes default. (3) 2026-06-24 gap "leap-cli + code-loader null baselines" **RESOLVED** — full-verified this run, real master SHAs recorded. Cross-checked during the run: `mapToEsFilters` still exists (web-ui `modelTestHelpers.tsx:48`), node-server still emits the `authenticated` socket frame (`utils/socket.ts:19`), web-ui still has no Cypress/Playwright e2e.

## 2026-06-29 — manual (targeted addition: collection sample-viewer grid)
Reviewed: web-ui `src/sample-collection/**` (CollectionViewDialog, CollectionVisualizationGrid, useStreamingSamplesVis, useStreamingSamplesVisStore, useCollectionSamplePage, useCollectionIndexStatus) + `src/ui/atoms` VirtualizationGrid, at web-ui master `f16249f1` (= current baseline). **Not a drift-sync** — filling a known coverage gap surfaced by BF-1060.
Changed: 03-data-flows.md — added **Flow C — Collection sample-viewer grid** (open → `ensureCollectionIndex`/`getCollectionSampleOrder` → virtual scroll renders cells live, vis fetch debounced to scroll-stop → `STREAMING_SAMPLES_VIS` in-memory render → socket push → FIFO vis cache) + collection-grid failure modes incl. the BF-1060 "keeps refreshing" gotcha and its 0-re-stream verification. 06-ui-inspection.md — cross-link from the Collections selector row. 09-job-catalog.md — Streaming Samples Vis now notes its UI consumer (`generateStreamingSamplesVis`, debounced/deduped) + Flow C link. manifest.json — mapped `web-ui/src/sample-collection/**`, `web-ui/src/ui/atoms/**`, `node-server/src/sampleCollections/**` onto 03/06/09.
Open gaps: (1) Front-end mechanics documented from a **code read**, not observed end-to-end on a fresh "building" index — re-confirm the exact `Indexing… X / Y` badge text and the `ensureCollectionIndex` poll cadence against a live cluster mid-backfill. (2) `generateStreamingSamplesVis` / `ensureCollectionIndex` / `getCollectionSampleOrder` are cited by endpoint name; node-server route handlers were not deep-verified this run (`node-server/src/sampleCollections/**` baseline unchanged). No baselines bumped (web-ui already at HEAD `f16249f1`).

## 2026-06-25 — manual refresh (engine / web-ui / helm-charts → master)
Reviewed: engine `9045cc33..026729cf`, web-ui `c0afae07..f16249f1`, helm-charts `8cbe68cb..05643d36` (all baselines were ancestors — clean diffs; 14 per-file verifiers).
Changed: 01-architecture.md — image tags (engine/engine-generic→`026729cf`, node-server→`9276bb7c`, web-ui→`f16249f1`) + chart version `1.6.28`→`1.6.33`. 03-data-flows.md & 07-failure-modes.md — exit 137 during pod **teardown** (`deletion_timestamp` set) is now classified `UNKNOWN`, not `OOM_KILLED`. 04 & 09 — added the new 7th `SLIM_LS` subtype **Resplitting** (`WorkerSlimLSOps.resplitting`, request `SlimResplittingRequest`, bucket `digest_<d>/resplitting/*`).
Open gaps: **Resplitting node-server side not shipped** — node-server master has no `resplit` reference, so the REST trigger / subType label / mongo entity / DS-Curation UI for resplitting don't exist yet; doc 09 documents the engine side and flags this. Re-verify when node-server wires it. No-change (internal/additive only): visualizationmanager, redis_vis_queue_manager, insights_calculation_manager, leaptrainer, visualizer_calculator, basestorage sample-vis helper, web-ui PE status hook (already matched after the node-server refresh), version-state.ts, and the installer Go files.

## 2026-06-25 — manual refresh (node-server → master)
Reviewed: node-server `1b44e3bb..9276bb7c` (now on `master`; baseline was an ancestor — 4 Population-Exploration commits #1757–#1760). Other repos not re-checked this run.
Changed: 09-job-catalog.md — Population Exploration: (a) PE now blocks only until the prerequisite evaluate's `insights_analysis` step finishes/skips (#1758), not for the whole evaluate job; (b) the digest no longer hashes `sample_visualizers_revision` (#1760).
Open gaps: none new. `storage.ts` changed the sample-visualizers folder `sample_visualizers_<N>` → `sample_visualizers/<N>` (#1757), but doc 09 uses the `sample_visualizers/*` glob so no edit was needed. node-server baseline bumped to `master` HEAD `9276bb7c`; the feature-branch caveat below is resolved.

## 2026-06-24 — initial
Reviewed: KB authored from helm-charts@8cbe68cb, engine@9045cc33,
node-server@1b44e3bb (since merged to master), web-ui@c0afae07.
Changed: all docs created (README, 01–10) + maintenance scaffolding.
Open gaps:
- ~~**node-server baseline was a feature branch**, not `master`.~~ **RESOLVED 2026-06-25** — reconciled to node-server `master` (HEAD `9276bb7c`); see the 2026-06-25 refresh entries above.
- **Graph jobs** (`DRY_RUN_GRAPH` vs `ANALYZE_GRAPH`, and how "Graph Validate"
  maps to each within the PUSH flow) were slightly conflated across recon sources —
  confirm precise spawn behavior and k8s names on a live cluster (noted in
  [09-job-catalog.md](../09-job-catalog.md#graph-jobs-two-distinct-things)).
- **SLIM_LS per-task memory ceilings** (insights/balancing load latent spaces in a
  single pod) — confirm OOM thresholds on a live cluster.
- **leap-cli + code-loader newly added to tracking** (2026-06-25) with null
  baselines — their docs (02/03/05/08/09) were authored from the first recon without
  these repos checked out, so the first `update-qa-knowledge` refresh should
  full-verify them and record real master SHAs. leap-cli's local checkout is on branch
  `fix-default-client-proxy`; track `master`.
- A few `file:line` anchors are snapshots and may have drifted; treat as
  named-symbol hints.
