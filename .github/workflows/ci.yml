name: CI

on:
  push:

jobs:
  lint-install-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run ShellCheck
        run: shellcheck ./install.sh
